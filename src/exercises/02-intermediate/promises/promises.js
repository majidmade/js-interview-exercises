import { getUser } from "./users";
import { getRankText } from "./ranks";

export const getUserRank = (name, wordedText = false) => {
  // Use getUser and getRankText to return user rank
  // if wordedText is true, return 'First' and 'Second' instead of '1st' and '2nd', respectively
};

export const getUserRanks = (names = []) => {
  // Use getUser to return user ranks.
  // call getUser for each name in the names array.
  // Returning arry does not have to be sorted or unique.
};